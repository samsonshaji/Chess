#include "human.h"
#include <iostream>
#include <sstream>
#include <string>

Human::Human(Colour colour) : Player(colour) {}

Move Human::makeMove(const Board &board) {
    //input is of the form "e2 e4"
    
}

